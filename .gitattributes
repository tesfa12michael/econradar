# Normalize line endings to LF in the repo; check out native on Windows.
# Keeps CI (Linux) and local (Windows) consistent and avoids CRLF churn.
* text=auto eol=lf

# Binary / generated assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
