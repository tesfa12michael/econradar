# EconRadar backend — dev/test dependencies.
# Install both: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.2,<9
pytest-asyncio>=0.23,<0.25
ruff==0.16.0  # pinned exactly: an unpinned range let CI drift and fail RUF100
